{
  "routes": [
    {
      "method": "GET",
      "path": "/tags",
      "handler": "api::tag.tag.find",
      "config": {
        "policies": [],
        "middlewares": []
      }
    },
    {
      "method": "GET",
      "path": "/tags/:id",
      "handler": "api::tag.tag.findOne",
      "config": {
        "policies": [],
        "middlewares": []
      }
    }
  ]
}
