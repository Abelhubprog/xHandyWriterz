{
  "routes": [
    {
      "method": "GET",
      "path": "/categories",
      "handler": "api::category.category.find",
      "config": {
        "policies": [],
        "middlewares": []
      }
    },
    {
      "method": "GET",
      "path": "/categories/:id",
      "handler": "api::category.category.findOne",
      "config": {
        "policies": [],
        "middlewares": []
      }
    }
  ]
}
